{
  "$schema": "https://openclaw.ai/schemas/plugin.json",
  "id": "now4real",
  "name": "Now4real",
  "description": "Connect OpenClaw to Now4real pagechat via webhooks",
  "version": "1.0.0",
  "kind": "channel",
  "channels": ["now4real"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "channelConfigs": {
    "now4real": {
      "schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Enable or disable the Now4real channel"
          },
          "webhookAuthorization": {
            "type": "string",
            "description": "Webhook authorization secret for verification"
          },
          "now4realApiKey": {
            "type": "string",
            "description": "Now4real API key used in the Authorization header for outbound API requests"
          },
          "openClawDisplayName": {
            "type": "string",
            "description": "Display name shown for the bot when replying"
          },
          "openClawDisplayIcon": {
            "type": "string",
            "description": "URL of the icon shown for the bot when replying"
          },
          "requireMention": {
            "type": "boolean",
            "description": "If enabled, replies are generated only when incoming messages mention @<Bot Display Name>",
            "default": false
          }
        },
        "required": ["webhookAuthorization", "now4realApiKey"]
      },
      "uiHints": {
        "enabled": {
          "label": "Enabled",
          "help": "Turn the Now4real channel on or off."
        },
        "webhookAuthorization": {
          "label": "Webhook Authorization",
          "help": "Specify the value for the HTTP \"Authorization\" header to authenticate requests sent from the Now4real cloud to your webhook.",
          "placeholder": "e.g. Bearer your-token-here",
          "sensitive": true
        },
        "now4realApiKey": {
          "label": "Now4real API Key",
          "help": "Specify the API key value used in the HTTP \"Authorization\" header when OpenClaw calls the Now4real API.",
          "placeholder": "e.g. Bearer your-now4real-api-key",
          "sensitive": true
        },
        "openClawDisplayName": {
          "label": "Bot Display Name",
          "help": "The name shown for the bot when it replies in the chat.",
          "placeholder": "e.g. Support Bot"
        },
        "openClawDisplayIcon": {
          "label": "Bot Display Icon",
          "help": "URL of the avatar image shown for the bot when it replies in the chat.",
          "placeholder": "e.g. https://example.com/bot-icon.png"
        },
        "requireMention": {
          "label": "Require Mention",
          "help": "When enabled, the bot replies only if the incoming message includes @<Bot Display Name>."
        }
      }
    }
  }
}
