{
  "name": "@openclaw/channel-now4real",
  "version": "1.0.0",
  "type": "module",
  "openclaw": {
    "extensions": ["./index.ts"],
    "setupEntry": "./setup-entry.ts",
    "channel": {
      "id": "channel-now4real",
      "label": "Now4real",
      "blurb": "Connect OpenClaw to Now4real pagechat."
    }
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
