{
  "name": "@now4real/now4real",
  "version": "1.0.0",
  "type": "module",
  "openclaw": {
    "extensions": ["./index.ts"],
    "setupEntry": "./setup-entry.ts",
    "channel": {
      "id": "now4real",
      "label": "Now4real",
      "blurb": "Connect OpenClaw to Now4real pagechat."
    }
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "peerDependencies": {
    "openclaw": "^2026.4.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "openclaw": "^2026.4.8",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
